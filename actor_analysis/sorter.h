#ifndef SORTER_H
#define SORTER_H

#include "IncludeLibraries.h"

using namespace std;

class Sorter
{
public:
    Sorter();
    void portSorter(Graph *graph);
    void portValueSorter(Graph *graph);
};

#endif // SORTER_H
